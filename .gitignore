bin/
obj/
/packages/
riderModule.iml
/_ReSharper.Caches/

# Local credentials. The repository is public, so no credential value is ever
# committed — only the *.example templates next to these paths, which document
# which keys are needed without carrying their values.
.env
Kipu.Platform/appsettings.Development.json

# Planning/handover notes are kept outside the repository on purpose: they are
# an up-to-date inventory of what this system does and where it is still weak,
# which is not something to publish alongside a deployed app.
docs/
